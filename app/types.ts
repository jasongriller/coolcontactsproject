export interface Contact {
    username: string;
    firstName: string;
    lastName: string;
    email: string;
    phoneNumber: string;
}

export interface SessionTokenResponse {
    sessionToken: string;
}
